spring.application.name=eventure-backend
# Configuración de PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/eventure_db
spring.datasource.username=diego_postgresql
spring.datasource.password=@Diegodomenpostgresql

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Puerto del servidor (opcional)
server.port=8080